import register from './register';
import bootstrap from './bootstrap';
import destroy from './destroy';
import services from './services';

export const ENCRYPTABLE_FIELD = 'encryptable-field';

export default {
  register,
  bootstrap,
  destroy,
  services,
};
